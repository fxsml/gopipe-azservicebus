# Create Feature

Create a new feature branch from develop.

## Steps

1. `git checkout develop && git pull origin develop`
2. `git checkout -b feature/$ARGUMENTS`
3. Report git status and confirm active branch

## Available Tools

Bash
